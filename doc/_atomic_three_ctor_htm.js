var list_across0 = [
'_contents.htm',
'_reference.htm',
'_index.htm',
'_search.htm',
'_external.htm'
];
var list_up0 = [
'cppad.htm',
'ad.htm',
'advalued.htm',
'atomic.htm',
'atomic_three.htm',
'atomic_three_define.htm',
'atomic_three_ctor.htm'
];
var list_current0 = [
'atomic_three_ctor.htm#Syntax',
'atomic_three_ctor.htm#Prototype',
'atomic_three_ctor.htm#atomic_user',
'atomic_three_ctor.htm#atomic_user.ctor_arg_list',
'atomic_three_ctor.htm#atomic_user.afun',
'atomic_three_ctor.htm#atomic_user.Implementation',
'atomic_three_ctor.htm#atomic_three',
'atomic_three_ctor.htm#atomic_three.Restrictions',
'atomic_three_ctor.htm#atomic_three.Base',
'atomic_three_ctor.htm#atomic_three.name',
'atomic_three_ctor.htm#Example',
'atomic_three_ctor.htm#Example.Define Constructor',
'atomic_three_ctor.htm#Example.Use Constructor'
];
function choose_across0(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_across0[index-1];
}
function choose_up0(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_up0[index-1];
}
function choose_down3(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_down3[index-1];
}
function choose_down2(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_down2[index-1];
}
function choose_down1(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_down1[index-1];
}
function choose_down0(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_down0[index-1];
}
function choose_current0(item)
{	var index          = item.selectedIndex;
	item.selectedIndex = 0;
	if(index > 0)
		document.location = list_current0[index-1];
}
