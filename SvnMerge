# script to help in execution of svn merge command
#
# execute this script form the directory corresponding to 
# http://www.coin-or.org/svn/CppAD/ 
cd ..
#
# change branches/Brad to the directory that will receive the changes
ToDirectory=branches/Brad
#
# change 101 to the version corresponding to the ToDirectory before
# the changes
StartRevision=101
#
# change trunk to the direcotry that contains the changes 
# (which have all been committed to the repository)
FromDirectory=trunk
# 
# change 114 to the version number corresponding to the FromDirectory
EndRevision=114
#
# echo the svn merge command
echo "cd  .."
echo "svn merge > SvnMerge.out \\"
echo "	http://www.coin-or.org/svn/CppAD/$ToDirectory@$StartRevision \\"
echo "	http://www.coin-or.org/svn/CppAD/$FromDirectory@$EndRevision \\"
echo "	$ToDirectory"
#
# execute the svn merge command
svn merge > SvnMerge.out \
	http://www.coin-or.org/svn/CppAD/$ToDirectory@$StartRevision \
	http://www.coin-or.org/svn/CppAD/$FromDirectory@$EndRevision \
	$ToDirectory
#
# put comment here so have newline at end of command above
